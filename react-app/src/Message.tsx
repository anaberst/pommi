// PascalCasing => used in React components
function Message() {
    // JSX: JavaScript XML (looks like HTML)
    const name = '';
    if (name) {
        return <h1>Hello {name}</h1>
    }
    return <h1>Hello World</h1>;
    
}

export default Message;